{
  "name": "instagram-follower-analyzer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "deploy:pages": "cd frontend && npm run build && wrangler pages deploy build --project-name instagram-follower-analyzer"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "bcryptjs": "^2.4.3",
    "hono": "^4.6.3",
    "hono-rate-limiter": "^0.4.2",
    "jszip": "^3.10.1",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
