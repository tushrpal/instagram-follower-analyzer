const config = {
  // In production set REACT_APP_API_URL to your Worker URL, e.g.
  // https://instagram-follower-analyzer-api.<your-subdomain>.workers.dev
  apiUrl: process.env.REACT_APP_API_URL || "",
};

export default config;
